Website for Lobo Metal LLc



**TO DO**
- gradually change the pixel values in the CSS to something more responsive
👋 add make the page more responsive to mobile and other screen sizes
- fix/keep working on the Centerpane. Notice that it's background is a different color 
   than the gridbox background. If this is deliberate, than I should probably add padding
   to the Centerpane. There is also a gap between the navbar and the gridbox that is 
   white instead of azure.
- - (on mobile) the hamburger button should be right justified, not centered. 
    also, it raises slightly when the navbar vertically expands. Need to fix this.
- - "About" and "Contact US" appear underlined in mobile.
👋 add an about section
👋 add a contact section
- - fix the sizing on the input fields. Also the input and textarea have different fonts.
- - turn the subject field into a drop-down.
👋 add a javascript page 
- - with support for scroll on-click